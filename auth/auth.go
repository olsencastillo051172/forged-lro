package auth

// Authenticate checks user credentials (stub)
func Authenticate(user string, password string) bool {
    return false
}

// Authorize checks if a role can perform an action (stub)
func Authorize(role string, action string) bool {
    return false
}
