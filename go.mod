module github.com/olsencastillo051172/forged-lro

go 1.22

// FORGED-LRO — Canon v1.0
// Scope: Specifications + audit scaffolding only
// No business logic included
// Multi-cloud, vendor-neutral, deterministic

require (
	// Optional testing helpers (may be removed if not used)
	github.com/stretchr/testify v1.9.0
)

